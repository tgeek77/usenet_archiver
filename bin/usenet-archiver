#!/usr/bin/env bash
# Dev launcher — run the package without installing (GUI by default; -c for CLI).
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
if [[ -x "$ROOT/.venv/bin/python" ]]; then
  exec "$ROOT/.venv/bin/python" -m usenet_archiver "$@"
fi
exec python3 -m usenet_archiver "$@"
